services:
  bot:
    build: .
    image: telegram-ai-bot
    container_name: telegram-ai-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/bot.db
    volumes:
      - ./data:/data
    stop_grace_period: 15s
